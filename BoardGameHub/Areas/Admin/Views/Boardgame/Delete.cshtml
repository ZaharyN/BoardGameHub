@model BoardgameDeleteFormModel
@{
    ViewBag.Title = "Delete Boardgame";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title text-center"><strong>@Model.Name</strong></h3>
                <p class="text-danger mt-3">"Are you sure you want to delete this boardgame?"</p>
            </div>
            <div class="card-footer">
                <form asp-action="DeleteConfirmed" method="post">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger">Confirm Delete</button>
                    <a asp-area="" asp-action="Active" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>